#ifndef LEXER_HPP
#define LEXER_HPP

#include <string>

class Lexer
{
   public:
    Lexer();
    ~Lexer();

    void lex(std::string const &path);

   private:
    // member fields
 
   private:
    // member functions
};

#endif
